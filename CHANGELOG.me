# Y-Dock AI Changelog

本文件记录 Y-Dock 面向 AI/维护者的工作流水。用户可见发布历史仍维护在 `CHANGELOG.md`；每次正式 release 必须同时更新 `CHANGELOG.md` 和 GitHub Release。

不要在这里记录 token、证书密码、Apple ID、notary 密码等敏感信息。

## 2026-07-23

- 准备发布 `v1.1.21 (46)`，同步更新 Xcode 版本、README 双架构下载链接、公开 changelog、AI_CONTEXT 与维护记录。
- 新增 Dock 主按钮点击最小化策略：默认关闭，可选择仅单窗口 App 或所有窗口；只响应无修饰键的主按钮点击，并严格要求目标 App 在点击前已经是前台，后台 App 点击继续保留系统原生激活行为。点击命中使用公开 Accessibility API 严格识别 `AXDockItem / AXApplicationDockItem`，多窗口模式批量最小化当前未最小化的用户窗口。
- 增加前台 App 快照与 `NSWorkspace.didActivateApplicationNotification` 时序保护，系统当前 PID、观察器 PID、最后 pointer-move 快照和激活时间必须共同确认目标在点击前已是前台；修复鼠标停在 A 后用键盘切到 B、再不移动鼠标点击 A 时可能误最小化的竞态。对应纯策略测试覆盖激活通知到达前后、激活后重新移动鼠标及各类 PID 不匹配。
- 预览卡片左上控制按钮新增可开关的 hover 放大效果，目标直径可在 `16.5–30pt` 范围连续调整并在设置页预览；按钮保持固定 `30pt` slot，只动画绘制直径，避免卡片和相邻按钮抖动。
- 新增隐藏式左上控制按钮：开启后按钮默认真实隐藏并禁用 hit-test，仅进入左上控制区域时显示；局部 tracking 不使用会忽略自定义 rect 的 `.inVisibleRect`，隐藏动画带 generation 防护并重置残留 hover 状态。
- 红色关闭按钮新增 graceful quit 策略，支持全部 App、黑名单和白名单三种模式；名单以 bundle identifier 独立持久化，切换模式或关闭总开关不会丢失。策略退出只调用 `NSRunningApplication.terminate()`；原紫色显式退出按钮和 Option+Tab 单窗口关闭入口保持原行为。
- 新设置默认全部关闭。standalone 策略测试分别以 thin `arm64`、`x86_64` 编译运行通过；`release.sh` 现会在读取签名/公证配置前，连同原 updater selector 测试一起执行两种架构的 AppSettings 策略测试并断言测试二进制为目标单一架构。
- `v1.1.21 (46)` 最新源码的 arm64 Debug、arm64 Release、x86_64 Release 无签名构建均成功，三个 App 主可执行文件分别确认为对应单一架构；原 updater selector 测试与新增设置策略测试也均在两种架构运行通过。设置页布局已通过截图检查。
- 正式交互冒烟发现控制 stack 从隐藏状态显示到指针下方时，子按钮 tracking area 不一定补发 `mouseEntered`，导致按钮显示但未放大；改为由卡片已有的 mouse enter/move/exit 同步命中三个固定 slot，并保留按钮自身 tracking 作为补充。修复后 arm64/x86_64 Debug/Release 无签名构建和双架构策略测试再次通过；真实指针悬浮截图确认仅当前紫色或红色按钮从 `16.5pt` 放大到设置的 `30pt`，其他按钮保持基础尺寸。
- 使用最终源码提交 `f2b5126` 重新运行 `release.sh` 成功。arm64 App/DMG 公证 ID 分别为 `e392e6a0-752c-421f-85ec-0c58751863eb`、`10f0f710-73b0-44ff-9a04-8b9cc213490f`；x86_64 App/DMG 公证 ID 分别为 `f712c561-d7e4-4532-bd30-247832994e6a`、`78f007d7-0dd6-49c4-9eab-61400fe86252`。最终 arm64 DMG 为 `1,665,197` bytes，SHA256 `74ef8781965cfa1fc1619cb615c4452c052dccc027aa780339372304812590b7`；x86_64 DMG 为 `1,692,509` bytes，SHA256 `b45f7898baccb6e44656fb1f5df0461a6e59f8ba011ddb5d9c2c402b9ff4e074`。两包均独立通过 Developer ID、App/DMG 公证与 staple、Gatekeeper、`hdiutil verify`、只读挂载及严格 thin 架构复核；最终 x86_64 DMG 中的 thin App 还通过 `/usr/bin/arch -x86_64` 在 Rosetta 下启动并持续存活 3 秒，随后主动终止并清理挂载点。
- 从上述最终 arm64 DMG 以 candidate + backup 方式安装 `/Applications/Y-Dock.app`，确认 `1.1.21 (46)`、Bundle ID `com.lixingchen.DockWindowPreview`、Team ID `A94225N8T5`、hardened runtime、严格 codesign、stapled ticket、Gatekeeper、thin arm64 和单一正式进程均通过。最终安装版真实验证隐藏式控制在卡片中央不显示、进入左上区域后显示、精确悬浮按钮放大，以及 whitelist 中红色按钮 graceful quit 整个 Probe App；此前同版本候选还已验证所有窗口最小化、仅单窗口模式、后台 App 点击不反向最小化及 Command 修饰键过滤。测试前用户偏好已逐项精确恢复，临时 Probe、测试副本、挂载点和安装回滚备份均已清理。
- 发布源码 tag `v1.1.21` 精确指向 `f2b5126`，后续验证记录提交未移动 tag；`master` 已推送。GitHub Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.21` 已按 arm64 在前、x86_64 在后的顺序公开，latest-release API 确认恰好两份资产，远端名称、顺序、bytes 与 SHA-256 digest 均与本地最终 DMG 一致；README 两个架构下载链接均返回可分段下载的 HTTP 206。

## 2026-07-22

- 准备安全补丁 `v1.1.20 (45)`：自动更新把 GitHub Release 的规范化版本贯穿到下载、挂载和安装脚本；只有严格高于当前版本且与下载 App 的 `CFBundleShortVersionString` 完全一致时才继续，旧包改名、同版本、降级和异常版本都会安全停止。
- 更新安装器移除 `rm -rf DEST && ditto`：挂载源、`/Applications` 同卷 candidate、现有 App 和最终 destination 都验证非 symlink 身份、Bundle ID、Developer ID 团队与 authority、hardened runtime、严格签名、Gatekeeper、目标版本和当前编译架构唯一 thin slice；正式替换使用 candidate + backup 原子 rename，候选复制、落位或最终验证失败时恢复原 App。
- 管理员权限 fallback 不再执行另一套删除复制命令，而是以 `privileged` 模式重新调用同一事务脚本；无权限时才进入授权路径，普通验证失败不会靠提权绕过。可信 installer SHA-256 直接从签名 App 内编译的脚本文本计算；root 只执行复制到 `/var/tmp` 后重新校验摘要的 root-owned 脚本。
- root 事务不再从可卸载/替换的 DMG mount 执行 helper：先把已验证 candidate 完整复制为 root-owned `Y-Dock-transaction-helper.app`，再次验证版本、身份、Developer ID、hardened runtime、codesign、Gatekeeper、strict thin 架构和主可执行摘要，并在每次 `RENAME_SWAP` / `RENAME_EXCL` 前复核。
- 普通与管理员事务增加目标目录固定 `.Y-Dock-update.lock`；direct installer 使用 `/bin/zsh -f`，写出 `READY\n` 后关闭 stdout，Swift `poll(2)` 在 60 秒内持续读取到 EOF 并要求完整帧严格相等。`READY\nCORRUPT`、不关闭 pipe、短帧、timeout 或取消都会终止 installer；TERM/INT/HUP 在父进程等待阶段撤销退出授权并执行 cleanup。
- 嵌入脚本通过 `zsh -n`，受控事务 harness 28/28 通过，覆盖普通/管理员成功与失败、rollback、mount helper 替换、复制 helper 篡改、已有事务锁、READY 后取消以及 Y-Clip 的恢复故障场景。另从 Y-Clip/Y-Dock 两份 Swift 源码提取实际 READY reader 编译测试，`READY\n`+EOF、尾随字节、开放 pipe、短帧和空帧共 10/10 通过；未触碰真实 `/Applications`。
- `UpdateVersionValidator` standalone 测试新增内部版本精确匹配、旧包改名、空 expectedVersion、同版本、降级和异常版本拒绝；arm64 与 x86_64 两种 target 均编译运行通过。最新 READY 修改后，arm64/x86_64 的 Debug 与 Release 都使用独立 `/tmp` DerivedData 无签名重新构建成功，Info.plist 均为 `1.1.20 (45)`，主可执行文件均严格为目标单一架构；正式发布仍会从最终提交重新执行完整构建。
- `release.sh` 的源码指纹改为 fail closed：确认独立 Git 根、显式检查 tracked/untracked NUL 枚举、直接写入 symlink 原始 target 字节（保留末尾换行）、为 tracked 工作区删除写入稳定 marker，并通过 stdin 计算普通文件 SHA-256，使反斜杠或换行文件名也能稳定进入摘要；完整 `.new` 双包验证后切换前及最终产物验证后成功标记前再次复核。正式发布在 baseline 前要求仓库内非 symlink 的 `Y-Framework` 共同祖先和 DMG vendored 子树，并以 `Y_RELEASE_REQUIRE_VENDORED=1` 禁止打包回退到父目录源码；强制模式固定使用仓库内背景生成器并清除外部布局变量。三项目指纹、异常文件名与 vendored 输入 harness 均通过，vendored harness 15/15 覆盖外部 generator、父目录 fallback、DMG symlink 和祖先 symlink。
- 从最终发布源码提交 `bb4e671` 运行 `release.sh` 成功：arm64 与 x86_64 的 Debug/Release App、App ZIP 和 DMG 均使用隔离路径，分别完成 Developer ID 签名、公证、staple、Gatekeeper、`hdiutil verify`、最终只读挂载、版本/build 与精确 thin 架构复核。唯一最终 arm64 DMG 为 `1,631,029` bytes，SHA256 `299545076340143edb67e1e753e0ad2f7ca735ad4b29914a867a253d2de9a011`；x86_64 DMG 为 `1,656,317` bytes，SHA256 `4eb6a9011a1730fa50d21ce8d023ee79e74e69d83dd43a7629ae27bad896baaa`。
- 从上述最终 arm64 DMG 以可回滚方式覆盖安装 `/Applications/Y-Dock.app`，确认 `1.1.20 (45)`、Team ID `A94225N8T5`、严格 codesign、Gatekeeper 与 arm64 thin 架构通过并恢复后台运行；x86_64 最终 DMG 中严格 thin 的可执行文件通过 Rosetta 短暂启动存活冒烟后已终止，临时实例、镜像和挂载点已清理。
- 发布源码提交和 tag `v1.1.20` 已推送；GitHub Draft Release 严格先上传并核对 arm64，再上传 x86_64。Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.20` 已公开，latest-release API 确认恰好包含上述两份 DMG，arm64 排在首位，远端名称、顺序、bytes、SHA-256 digest 与本地最终文件一致；远端 tag 精确解析到 `bb4e671`，后续维护记录提交不移动 tag。
- 将 `release.sh` 改为单次发布分别构建严格 thin 的 `arm64` 与 `x86_64` Debug/Release App：每种架构使用独立 DerivedData、stage、App ZIP、DMG 工作目录，分别执行签名、公证、staple、Gatekeeper、最终只读挂载复核和精确架构断言；输出固定为 `Y-Dock-v$VERSION-arm64.dmg` 与 `Y-Dock-v$VERSION-x86_64.dmg`，不允许把旧产物复用为新版本输入。
- 新增纯逻辑 `UpdateAssetSelector`，按编译架构精确匹配完整 GitHub asset 名称；更新检查不再选择第一个 `.dmg`。缺少当前架构资产时禁用直接安装并明确引导 Release 页面，不会回退安装其他架构或无关 DMG。
- 自动更新在挂载并定位下载 App 后，会先验证 `Contents/MacOS/Y-Dock` 是当前编译架构唯一 slice 的 thin binary，再执行代码签名、Gatekeeper 和身份验证；只读挂载源在安装脚本删除旧 App 前再次做同一架构断言。错架构或 universal binary 会安全失败，现有 `/Applications/Y-Dock.app` 不会被删除。
- 新增资产与主可执行文件架构纯逻辑测试，覆盖编译架构、资产顺序反转、无关 DMG、目标架构缺失、正确 thin、错误 thin、universal、重复和空架构输出；测试与 App 共用同一实现。
- `release.sh` 在读取签名/公证配置和任何签名动作前先编译运行 `arm64`、`x86_64` standalone 测试；最终 dist 阶段先成套预检两个 source DMG，再复制到同目录 `.new`、完整复核 DMG 和挂载 App，然后备份并成套切换两个正式文件。复制、验证、备份、改名或最终复核任一步普通失败都会清理本轮文件并恢复原有完整资产组；回滚模拟已通过。新增同版本原子目录锁，拒绝并发发布交错操作同一组正式文件；记录仓库源码指纹并在每个架构构建前后及最终切换前复核，中途源码变化会停止发布，避免双包来自不同源码。异常崩溃遗留锁时会阻止后续流程静默覆盖现场。
- 首次双资产迁移使用 Draft Release：先上传 `arm64` 并确认它是草稿中的首个 `.dmg`，再上传 `x86_64`；两份资产和 digest 完整验证后才公开，并通过 GitHub latest-release API 确认首个 `.dmg` 仍为 arm64，以兼容仍会选择首个 DMG 的 v1.1.18 updater。Release 只能包含这两个 thin 包，不得上传通用名或 universal DMG。
- 准备 `v1.1.19`，版本号更新为 `MARKETING_VERSION=1.1.19`、`CURRENT_PROJECT_VERSION=44`，README、AGENTS、AI_CONTEXT 和公开 changelog 已同步双架构资产名称与流程。
- 最终无签名验证通过：扩展后的 standalone 测试分别编译并运行于 thin `arm64` 与 `x86_64`，全部通过；两种架构的 Release App 也分别使用独立 `/tmp` DerivedData、`ARCHS=<arch>`、`ONLY_ACTIVE_ARCH=YES` 构建成功，Info.plist 均为 `1.1.19 (44)`，主可执行文件分别严格为对应单一架构。两次构建仅出现未依赖 AppIntents 时跳过元数据提取的 Xcode 提示。
- 从最终发布源码提交 `e8d2eff` 运行 `release.sh` 成功：两个架构的 Debug/Release App、App ZIP、DMG 均使用隔离路径，分别完成 Developer ID 签名、公证、staple、Gatekeeper、`hdiutil verify`、最终只读挂载和精确 thin 架构复核。唯一最终 arm64 DMG 为 `1,616,914` bytes，SHA256 `5d3fac2e9411951baf21ab0ddf14c107cf579306a3c3fc8d456d615226341584`；x86_64 DMG 为 `1,642,796` bytes，SHA256 `1efc55af5e3cb42409577e63c8432377e17617316c935db309db70901949c62e`；此前重新生成前的摘要已作废，不得用于上传或校验。
- 从上述最终 arm64 DMG 以可回滚方式覆盖安装 `/Applications/Y-Dock.app`，确认 `1.1.19 (44)`、Team ID `A94225N8T5`、严格 codesign、stapled ticket、Gatekeeper 与 arm64 thin 架构通过并恢复后台运行；上述 x86_64 最终 DMG 中严格 thin 的 App 通过 Rosetta translated 模式短暂启动冒烟后已终止，镜像和临时挂载点已清理。
- 发布源码提交和 tag `v1.1.19` 已推送；GitHub Draft Release 已严格先上传 arm64、确认其为首个且唯一 DMG 后再上传 x86_64，远端两份资产的名称、顺序、bytes、SHA256 digest 和真实换行 release notes 均与上述最终文件一致。公开前的 Draft 核验阶段未读取或输出公证/GitHub 凭据。
- 完成 `v1.1.19` 正式发布闭环：Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.19` 已公开；GitHub latest-release API 确认返回 tag `v1.1.19`，且恰好包含 arm64、x86_64 两份 DMG，首个 `.dmg` 仍为 arm64，远端顺序、bytes 与 SHA256 digest 均未变化。远端 tag 解析结果与本地 `e8d2eff` 发布源码提交一致；后续维护记录提交不移动 tag。
- 从已提交源码重新生成最终双架构包时，Apple CloudKit ticket 服务曾在 x86_64 DMG staple 返回 SSL `-1200`，下一轮又遇到签名时间戳服务暂时不可用；两次均在写入 dist 前安全失败。`release.sh` 现对带时间戳的签名和 App/DMG staple 增加最多 4 次有限退避重试，持续失败仍停止发布，不降低签名、公证或验证要求。

## 2026-07-21

- 修复 Dock hover 偶发延迟不稳或鼠标停住后不再恢复的问题：鼠标节流改为保留最后一次位置的 trailing-edge 解析；Dock Accessibility 短暂 miss 会在有限时间内重试，并为已识别候选保留短暂容错，不再立刻清空候选和重启悬停计时。自动隐藏或放大 Dock 的兜底检测厚度从 96 扩到 128 points，AX 坐标优先使用正确的顶部原点换算点。
- Dock 预览改为先用缓存或轻量占位卡片准时显示，真实缩略图在后台逐张更新，避免主线程同步截图让实际出现时间随窗口数量波动。
- 多行 Dock 窗口卡片改为从底部向上分组，保持窗口顺序的同时形成“上少下多”的布局。
- `Option+Tab` 恢复 Windows 风格首次选择：正向呼出在存在多个窗口时默认选中第二张，只有一张时安全回退到第一张；后台补入最小化窗口后继续按 window ID 保持当前选择。
- `Option+Tab` 新增过滤型 `CGEventTap`：切换器打开时吞掉 Esc key-down、自动重复及对应 key-up，再取消面板，避免 Esc 到达底层 App；event tap 不可用时不会打开无法安全取消的切换器。
- 简化根目录及 Y-Clip、Y-Dock、Y-Keys 的 `AGENTS.md`：只处理实际修改的项目，共享框架只有同步进具体 App 才扩大范围，验证按风险和受影响路径执行，纯文档/分析/本地实验不自动触发 App 发布。本次用户可见代码仅修改 Y-Dock，因此只构建、验证和发布 Y-Dock，不重新发布 Y-Clip 或 Y-Keys。
- 准备发布 `v1.1.17`，版本号为 `MARKETING_VERSION=1.1.17`、`CURRENT_PROJECT_VERSION=42`；README、公开 changelog 和维护说明已同步。
- Debug 与 Release 无签名构建均通过；基于最终源码运行 `release.sh` 完成 App/DMG 双公证与装订，App 公证 ID 为 `fdf97830-247a-4736-afd2-89db0bfec496`，DMG 公证 ID 为 `90baa57c-c75a-4d71-9447-534db3e2cc7e`。最终 `Y-Dock-v1.1.17.dmg` SHA256 为 `b6275ad3102c60d45124762fd0702f6457c3e63646c977cf7da4004d6c66bb39`，Developer ID、hardened runtime、严格签名、App/DMG stapled ticket、Gatekeeper 与 `hdiutil verify` 均通过。
- 从最终 DMG 覆盖安装 `/Applications/Y-Dock.app`，确认正式安装版为 `1.1.17 (42)`、Bundle ID `com.lixingchen.DockWindowPreview`、`LSUIElement=true`、Developer ID 团队 `A94225N8T5`，且只运行一个正式进程。
- 使用 6 个 disposable Probe 窗口复测 Dock hover：连续 8 次从 Dock 外移动到同一图标都无需点击即可出现，测得 `min=110ms / avg=152ms / max=204ms`；layer 999 截图确认先显示面板并完成后台缩略图更新，多行布局为上方 2 张、下方 4 张，符合“上少下多”。
- 使用独立键盘事件 Probe 真实发送 Option-down、Tab、Esc key-down、Esc autorepeat、Esc key-up、Option-up：Y-Dock 日志确认首次 `selected index: 1`，底层 Probe 未生成任何 Esc 事件记录，取消后前台 Probe 窗口保持不变。
- 完成 `v1.1.17` 发布闭环：对应源码提交 `63d0e0745114d3529af0533545d8b7be09a4b78a`、tag `v1.1.17`、Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.17`。GitHub Release 标题、真实换行说明、资产 `Y-Dock-v1.1.17.dmg`、下载链接和 `sha256:b6275ad3102c60d45124762fd0702f6457c3e63646c977cf7da4004d6c66bb39` digest 均已复核一致；tag 保持指向与最终 DMG 对应的源码提交，发布记录使用后续文档提交，不移动 tag。
- `Option+Tab` 卡片新增 hover 关闭入口：关闭按钮与左上角 `25×25` App icon 完全重合，使用黑色 `0.50 alpha` 半透明遮罩和白色 X，不使用红色、额外边框或独立圆形按钮。
- 点击关闭按钮复用 Accessibility 的窗口关闭能力，不触发卡片激活；关闭后按 window ID 保留原选中项，关闭当前选中项时选择相邻窗口，最后一项关闭后退出会话。当前会话记录已关闭 window ID，避免异步补入最小化窗口时重新出现。
- 准备发布 `v1.1.18`，版本号为 `MARKETING_VERSION=1.1.18`、`CURRENT_PROJECT_VERSION=43`；README、公开 changelog 和维护说明已同步。
- 基于最终源码运行 `release.sh` 完成 App/DMG 双公证与装订：App 公证 ID 为 `3dc354be-9dd8-488f-bcd3-19dabb4d2152`，DMG 公证 ID 为 `13257f2a-d7f0-4209-8729-fdaa3c03178d`；最终 `Y-Dock-v1.1.18.dmg` SHA256 为 `c5de81216cb16912a1ff23dc01b5ea7f203070564601b6675f7a90b00c5d7a67`，Developer ID、hardened runtime、严格签名、App/DMG stapled ticket、Gatekeeper 与 `hdiutil verify` 均通过。
- 从最终 DMG 覆盖安装 `/Applications/Y-Dock.app`，确认正式安装版为 `1.1.18 (43)`、Bundle ID `com.lixingchen.DockWindowPreview`、`LSUIElement=true`、Developer ID 团队 `A94225N8T5`，且只运行一个正式进程。
- 使用受控双窗口 Probe 验证最终安装版：`Option+Tab` layer 1000 面板显示两张受控卡片；hover 后左上角出现与 App icon 重合的中性半透明黑色遮罩和白色 X。点击关闭后仅目标窗口消失，面板继续显示并保留剩余项，前台仍是独立的受控 Anchor App，确认关闭操作未误激活目标窗口；Esc 取消后 Option 修饰键已主动释放。
- 完成 `v1.1.18` 发布闭环：对应源码提交 `d593ab7a993a7b028ca39bbe30b3f9f4bc742f2d`、tag `v1.1.18`、Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.18`。GitHub Release 标题和真实换行说明正确，资产 `Y-Dock-v1.1.18.dmg` 的远端 digest 为 `sha256:c5de81216cb16912a1ff23dc01b5ea7f203070564601b6675f7a90b00c5d7a67`，与本地最终 DMG 完全一致；tag 保持指向功能源码提交，发布记录使用后续文档提交，不移动 tag。

## 2026-07-20

- 同步新版共享 APFS DMG 框架：背景改为浅色高对比设计并默认生成 `1280×800 / 144 DPI` 的 2x Retina PNG，按 App 与 Applications 的真实 Finder 坐标绘制居中箭头；安装窗口显式隐藏工具栏、状态栏、路径栏和标签页栏，防止内容区高度变化造成图标与箭头偏移。
- 背景资源从普通 `.background` 迁移到 Finder 特殊隐藏的 `.Trashes/bg.png`；可写镜像保存布局后清除 `.fseventsd`、索引和临时卷元数据，最终只读镜像会验证背景像素/DPI、完整图标坐标和窗口栏状态，并断言旧元数据目录不存在。
- 共享框架已通过包含隐藏兼容 App 的完整 APFS UDZO 测试；最终只读重挂载后的严格签名、BSD hidden、画外兼容图标坐标、可见图标坐标及元数据断言全部通过。深色系统外观截图确认 Finder 黑色名称清晰可读、箭头与两枚图标纵向中心对齐，左上角不再显示 `.background` 或 `.fseventsd`。
- 准备发布 `v1.1.16`，版本号为 `MARKETING_VERSION=1.1.16`、`CURRENT_PROJECT_VERSION=41`；README、公开 changelog 和维护说明已同步。
- 基于最终源码运行完整 Debug/Release 构建和正式发布流程：App 公证 ID 为 `889aa993-4c92-4891-92dd-685b72ceeeae`，DMG 公证 ID 为 `ef4706fa-ad6b-4da3-b5ec-6606e635cab5`；最终 `Y-Dock-v1.1.16.dmg` SHA256 为 `cdadf37e87c287da544fb44c2aaa3a219ef212e464872b849326de8aa19394b7`，Developer ID、hardened runtime、严格签名、App/DMG stapled ticket、Gatekeeper 与 `hdiutil verify` 均通过。
- 完成 `v1.1.16` 发布闭环：对应源码提交 `8afea9832c955317fd31072c3fad39f1c1d00cbd`、tag `v1.1.16`、Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.16`。GitHub Release 的 tag、标题、真实换行 release notes、资产 `Y-Dock-v1.1.16.dmg`、下载链接和 asset digest 均已复核，远端 digest 与本地 SHA256 完全一致。
- 从上述最终签名、公证并 stapled 的 DMG 覆盖安装到 `/Applications/Y-Dock.app` 并启动；确认正式安装版为 `1.1.16 (41)`、Bundle ID `com.lixingchen.DockWindowPreview`、`LSUIElement=true`、Developer ID 团队 `A94225N8T5`，hardened runtime、严格 codesign、App/DMG stapled ticket 与 Gatekeeper 均通过。
- 最终安装版通过 Dock Accessibility 命中 TextEdit：layer 999 hover 面板完整显示 12 张真实窗口卡片，鼠标移出后面板关闭。本轮未重新点击卡片切换用户窗口，避免干扰现有工作；卡片显示、AX 内容和关闭行为均已验证。
- 真实按下 Option+Tab 后 layer 1000 切换器正常出现，Esc 取消后前台 App 和聚焦窗口保持不变。菜单栏入口、`Y-Dock v1.1.16` 设置页、“正式安装版”、辅助功能与屏幕录制“已开启”以及 GitHub Release 更新入口均已复核。

- 重做 Y-Dock 菜单栏图标为透明 `18 x 18` template image：两个错位窗口轮廓表达多窗口预览，底部圆头横线表达 Dock，替代原来视觉偏重的实心标记。
- 保持 `LSUIElement=true` 和菜单栏后台工具形态不变，不修改 Y-Dock 应用 Logo；新模板图标会跟随 macOS 自动适配浅色/深色菜单栏。
- 加固 `release.sh`：公证等待连接中断但已取得 submission ID 时改用内建 `notarytool wait` 继续等待最终状态；同时校验 Debug/Release 的版本与构建号、签名标识和 Developer ID 类型。失败时清理大型 DerivedData/stage，只保留 DMG 与公证日志用于诊断。
- 准备发布 `v1.1.15`，版本号为 `MARKETING_VERSION=1.1.15`、`CURRENT_PROJECT_VERSION=40`；README、公开 changelog 和维护说明已同步。
- 完成 `v1.1.15` 发布闭环：源码提交 `f7c63c9f338f408175549a292c3687e55bd05863`、tag `v1.1.15`、Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.15`；最终 `Y-Dock-v1.1.15.dmg` SHA256 为 `77502e352e8b59b6071be6e5facffa29e1de73e8beb42fdf2d68723e1750d2d6`，App 公证 ID 为 `568a6d58-f28c-400c-8f6c-1bf1c9a2fd3e`，DMG 公证 ID 为 `983822c6-70a8-42d0-96de-a4a41bd15f22`。GitHub Release 的 tag、标题、真实换行 release notes、资产名称、下载链接和 asset digest 均已复核，远端 digest 与本地 SHA256 完全一致。
- 从最终 DMG 覆盖安装到 `/Applications/Y-Dock.app` 并启动；确认正式安装版为 `1.1.15 (40)`、`LSUIElement=true`、Bundle ID `com.lixingchen.DockWindowPreview`、Developer ID 团队 `A94225N8T5`，hardened runtime、严格 codesign、App/DMG stapled ticket、Gatekeeper 与 DMG 镜像校验全部通过。
- 菜单栏中的新版 `18 x 18` template 图标已在真实菜单栏复查：双层窗口轮廓与底部 Dock 横线清晰，透明背景正常；设置权限页正确识别当前副本为正式安装版，辅助功能和屏幕录制权限独立显示，更新入口显示 `v1.1.15`。
- 使用真实 TextEdit 多窗口验证 Dock hover：Accessibility 命中 `AXDockItem / AXApplicationDockItem` 后，Y-Dock layer 999 预览面板显示多张真实窗口卡片。点击首张卡片后，前台应用为 `com.apple.TextEdit`、聚焦窗口为 `未命名9`，预览面板正确关闭。
- 使用真实 `Option+Tab` 输入验证 layer 1000 窗口切换器，运行日志记录 MRU `selected index: 0`；按下 Esc 后切换器关闭，前台应用和聚焦窗口仍为 TextEdit / `未命名9`，确认取消操作未误激活其他窗口。

## 2026-07-19

- 将 `Y-Framework/Permission/YPermissionPromptFramework.swift` 加入 Xcode target Sources，统一首次启动和后续 Accessibility、Screen Recording 权限弹窗；共享协调器处理状态指纹、重复抑制、正式安装副本检查与顺序提示，Y-Dock 继续负责公开权限 API 和 `missing / restartRequired / active` 映射。
- `DockWindowPreviewApp` 在重新激活时调用共享协调器，用户从 System Settings 返回后会自动继续处理第一项尚未完成的权限；显式“请求隐私权限”仍可强制显示当前步骤。
- 新增仓库内 vendored 的 `Y-Framework/DMG` 和薄 `make_dmg.sh`，`release.sh` 改用共享背景与 Finder 布局流程，动态显示 Y-Dock 名称并验证 `.DS_Store`、Applications 链接、签名、图标位置和最终压缩镜像。
- 正式安装版重启改为通过 LaunchServices 打开精确 App 路径，不再使用会强制新实例的 `open -n`。
- 通过共享框架独立编译、Xcode Debug/Release 无签名构建、Y-Dock DMG 生成与背景/布局截图检查；准备发布 `v1.1.13`，版本号为 `MARKETING_VERSION=1.1.13`、`CURRENT_PROJECT_VERSION=38`。
- `v1.1.13` 的 DMG 已签名、公证并装订，但从最终 DMG 安装后复查发现 App 本体没有嵌入 stapled ticket；虽然 Gatekeeper 在线校验通过，仍不满足项目要求的 App/DMG 双重公证装订闭环，因此保留已发布 tag，不移动或复用该版本，并立即准备补丁版。
- `release.sh` 新增 App ZIP 公证、App ticket 装订、严格 ticket 文本校验和最终 DMG 重挂载后的 App ticket 复查；`stapler validate` 仅返回成功退出状态但输出“没有票据”时现在会明确失败。
- 准备发布 `v1.1.14`，版本号为 `MARKETING_VERSION=1.1.14`、`CURRENT_PROJECT_VERSION=39`；README 和公开 changelog 已切换到补丁版下载链接。
- 完成 `v1.1.14` 发布闭环：源码提交 `83375e4d49d78f3504788a32e800594d68a14ce5`、tag `v1.1.14`、Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.14`；最终 `Y-Dock-v1.1.14.dmg` SHA256 为 `c5cef7786fc8d3c2148a2a00e914e1d06f82f0eef490bc778360ae72692f007e`，GitHub asset 名称、下载链接和 digest 已核对一致，App 公证 ID 为 `edd04ff9-c623-444c-b511-dc302eb88b66`，DMG 公证 ID 为 `04bcfc3f-f5c2-4780-8afa-5bfd02dc8426`；已发布的 `v1.1.13` tag 和 Release 保持不变。
- 从上述最终 DMG 覆盖安装到 `/Applications/Y-Dock.app` 并通过 LaunchServices 启动；确认版本 `1.1.14 (39)`、Bundle ID `com.lixingchen.DockWindowPreview`、Developer ID 团队 `A94225N8T5`、hardened runtime、严格 codesign、App/DMG stapled ticket 与 Gatekeeper 均通过，且只运行一个 `Y-Dock` 进程。安装版权限页显示“正式安装版”、辅助功能和屏幕录制均“已开启”；设置页和更新入口正常，更新检查在瞬时 SSL 网络错误后重试成功并显示“最新版 1.1.14”；真实 `Option+Tab` 显示完整多窗口切换器且首张卡片被选中，Esc 可取消，Dock 悬停 TextEdit 时显示三张窗口预览卡片并可点击切换到目标窗口。

## 2026-07-18

- 共享设置框架增加屏幕可见区域约束，设置窗口在紧凑、缩放或远程显示环境中不再超出可见区域。
- 共享设置框架增加 sidebar identifier 校验，未知的设置预览页面不会再产生“无选中项但显示通用页”的不一致状态。
- 权限页新增运行副本诊断，按 `/Applications/Y-Dock.app` 路径、Bundle ID、代码签名有效性和 Developer ID 团队标识区分“正式安装版”与“开发副本”，并可切换到验证通过的安装版。
- 屏幕录制权限改为 `missing / restartRequired / active` 三态：未授权时提供请求，授权写入但当前进程尚未生效时显示“需要重启”，正式安装版提供“重启”，开发副本提供“切换安装版”。
- `Accessibility` 与 `ScreenCapture` 的 TCC 刷新均限定到 `com.lixingchen.DockWindowPreview`，后台执行并带超时处理，不会全局重置其他 App。
- 清理误恢复且未加入 target 的旧 `SettingsComponents.swift`，继续以 `YSettingsFramework.swift` 作为唯一设置组件体系。
- 修正屏幕录制引导状态：仅打开 System Settings 不再提前标记“需要重启”；初次引导会先调用对应权限请求 API，且只在 `CGRequestScreenCaptureAccess()` 已返回授权但 preflight 仍为 false 时记录待重启状态。
- 开发副本处于屏幕录制待重启状态时，只有检测到 Bundle ID、签名和 Developer ID 团队均有效的 `/Applications/Y-Dock.app` 才启用切换操作。
- 移除 Y-Dock 自己的设置预览截图实现，统一使用共享设置框架提供的截图逻辑。
- 自动更新在替换前挂载并验证下载 App：拒绝符号链接以及 Bundle ID/Developer ID 团队不匹配的来源，同时保留 DMG/App 的 `spctl` 与 `codesign` 校验；缺失 `/Applications/Y-Dock.app` 时统一返回正式安装路径指引。
- `release.sh` 现在强制构建 Debug 和 Release，并只清理本次调用创建的临时 DerivedData、stage 和挂载目录，不再删除其他流程可能创建的 `DebugDerivedData` / `VerifyDerivedData`。
- 新增项目级 `CLAUDE.md`，继续由 `AGENTS.md` 统一维护发布和本机验收要求。
- 准备发布 `v1.1.12`，版本号为 `MARKETING_VERSION=1.1.12`、`CURRENT_PROJECT_VERSION=37`，公开 changelog 日期为 `2026-07-18`。
- 完成 `v1.1.12` 发布闭环：源码提交 `b5c6c0bd888f9ae3027e2909db2ba22f0d51def8`、tag `v1.1.12`、Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.12`；最终 `Y-Dock-v1.1.12.dmg` SHA256 为 `ec476ac089900e12f0e9cb145dfd65abb6563935bed36c15c4cfaf715e6723fc`，GitHub asset digest 已核对一致。
- 从上述最终 DMG 覆盖安装到 `/Applications/Y-Dock.app` 并通过 LaunchServices 启动；确认版本 `1.1.12 (37)`、Bundle ID `com.lixingchen.DockWindowPreview`、Developer ID 团队 `A94225N8T5`、hardened runtime、严格 codesign、DMG stapler 与 App/DMG Gatekeeper 验证通过。安装版权限页截图确认“正式安装版”、辅助功能和屏幕录制均“已开启”，且只运行一个 `Y-Dock` 进程。

## 2026-07-13

- 修复 `Option+Tab` 首次呼出时固定选中第二张卡片的问题，改为从当前展示列表的第一张开始选择。
- 将所有可见和最小化窗口统一改为严格 MRU 聚焦历史排序；CoreGraphics 当前 Z-order 只用于尚未记录过焦点的新窗口兜底。
- 为运行中的普通 App 注册公开 Accessibility 聚焦窗口/主窗口通知，并保留 `300ms` 轻量轮询作为漏报兜底。
- 增加 AX 完整标题与 CGWindow 省略号截断标题的兼容匹配，避免 Edge/Chromium 多窗口历史记录串位。
- 准备发布 `v1.1.11`，版本号更新为 `MARKETING_VERSION=1.1.11`、`CURRENT_PROJECT_VERSION=36`，并同步更新 README、`AI_CONTEXT.me` 和 `CHANGELOG.md`。
- 发布 `v1.1.11` 的签名、公证、staple 后 DMG，SHA256：`e53b645f9a335a78d08a17b2ecaa5ec36496c53163dc4908bca9eb68ddb01c75`。
- 完成 `v1.1.11` 发布闭环：源码提交 `a79b5f9`、tag `v1.1.11`、Release `https://github.com/Rainchen537/Y-Dock/releases/tag/v1.1.11`；最终 DMG 已覆盖安装到 `/Applications/Y-Dock.app`，确认版本 `1.1.11 (36)`、Developer ID/公证/Gatekeeper、辅助功能与屏幕录制权限、Dock 多窗口预览、设置权限页及更新检查均正常。真实 `Option+Tab` 日志确认首次 `selected index: 0`，并观察到聚焦后的 Edge 窗口从历史列表后部移动到首位、其余窗口保持原有 MRU 顺序。
- 新增项目级 `AGENTS.md`，继承 Y-Project 根规范，并记录 Y-Dock 的仓库、分支、Xcode 版本字段、DMG 命名和本机安装验证要求。
- 将“每次修改任务必须发布新版本”设为强制完成条件：签名、公证并 staple DMG，发布 GitHub Release，再从最终 DMG 覆盖安装本机 App。
- 修复 Edge/Chromium 多窗口激活不稳定问题：当 CGWindow 标题被省略号截断、AXWindow 标题为完整浏览器标题时，使用省略号通配和 token fuzzy matching 识别目标窗口。
- 避免复用只有几何尺寸命中的旧 AXWindow 缓存，降低两个浏览器窗口同位置同尺寸时选错窗口的概率。
- 强化窗口激活顺序：对目标 AXWindow 重复执行 frontmost、raise、focusedWindow、main 和 focused 设置，提升无痕窗口等同 App 多窗口置前稳定性。
- 重新确认 `Y-Framework/Setting/YSettingsFramework.swift` 已加入 target Sources，并移除对未跟踪旧 `SettingsComponents.swift` 的 target 依赖。
- 准备发布 `v1.1.10`，版本号更新为 `MARKETING_VERSION=1.1.10`、`CURRENT_PROJECT_VERSION=35`，并同步更新 README 和 `CHANGELOG.md`。
- 发布 `v1.1.10` 的签名、公证、staple 后 DMG，SHA256：`59f415e321c704378410e91aeb216bee3df4e85ccd9975756fd337bb9327b063`。

## 2026-07-10

- 将 GitHub 仓库从 `Rainchen537/DockWindowPreview` 重命名为 `Rainchen537/Y-Dock`。
- 同步更新本地 `origin`、README 仓库记录与发布链接、项目主页链接及 GitHub Release 自动更新接口。

## 2026-06-28

- 接入 `Y-Framework/Setting/YSettingsFramework.swift` 共享设置页面框架，并加入 Xcode target Sources。
- 在仓库内 vendored `Y-Framework/Setting/YSettingsFramework.swift`，避免单独 clone GitHub 仓库时缺少父级共享框架。
- 将原菜单栏 popover 设置页重构为独立设置窗口，采用左侧 sidebar + 右侧内容区结构。
- 将设置页 sidebar 统一为 `通用 / 功能 / 权限 / 更新 / 关于`，把预览参数、系统和权限等旧分类收敛到右侧 section card。
- 调整共享设置框架窗口比例、sidebar 宽度、内容内边距和卡片等宽约束；通过截图检查 Y-Dock 权限页按钮行没有横向溢出。
- 将共享设置窗口默认尺寸放大到 `1200 x 810`，并把左侧导航按钮改为居中排列。
- 修正 sidebar 内 App 信息区和导航区的居中方式，改为直接按 sidebar 中心线约束；移除设置页页面标题和 section card 内的静态解释性小灰字。
- 将 sidebar App 信息区顶部间距调为 96，让 logo 保持在左侧上半部分。
- 将菜单栏入口改为简单菜单：打开设置、更多 Y-Project 占位和退出项。
- 保留原有预览参数、系统、权限和关于/更新业务逻辑。
- 移除旧的本地 `SettingsComponents.swift`，避免和共享设置框架形成两套组件体系。
- 新增 `release.sh`，固化 Release 构建、Developer ID 签名、DMG 签名、公证、staple 和 Gatekeeper 验证流程。
- 准备发布 `v1.1.9`，版本号更新为 `MARKETING_VERSION=1.1.9`、`CURRENT_PROJECT_VERSION=34`，并同步更新 README 和 `CHANGELOG.md`。
- 发布 `v1.1.9` 的签名、公证、staple 后 DMG，SHA256：`aac81e451b122cb14e704731e2e23fedb4f4f8b5d539a308f3d9d4383e4f57f6`。

## 2026-06-27

- 新增规范化主入口 `AI_CONTEXT.me`，并引用根目录 `Y_PROJECT_APP_STANDARD.me`。
- 保留原 `AI_MAINTENANCE.me` 作为兼容入口，指向新的 `AI_CONTEXT.me`。
- 单独新增 `CHANGELOG.me`，用于记录 AI/维护者工作流水；用户可见版本历史继续使用 `CHANGELOG.md`。
- 在发布流程中补充 GitHub MCP / GitHub App 优先创建 release、上传 DMG，以及发布后本机安装验证的要求。

## 2026-06-22

- 新增原始 `AI_MAINTENANCE.me`，记录项目架构、构建验证、签名公证、GitHub release 流程和维护注意事项。
- 新增 `CHANGELOG.md`，用于用户可见版本历史，并在 README 中链接。
